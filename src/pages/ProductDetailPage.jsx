/* eslint-disable */
function ProductDetailPage({ productDetails }) {
  return (
    <div>
      ProductDetailPage
      <p>
        {productDetails.name}
      </p>
    </div>

  );
}

export default ProductDetailPage;
